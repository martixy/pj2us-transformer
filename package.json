{
  "name": "pj2us-transformer",
  "version": "0.1.0",
  "description": "Transforms package.json into userscript metadata, including a local require, if provided",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "martixy",
  "license": "MIT",
  "dependencies": {
    "brototype": "^0.0.6",
    "file-url": "^2.0.2"
  }
}
